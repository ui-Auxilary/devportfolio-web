import styled from "styled-components";

const Diagonal = styled.div`
    position: absolute;
    top: 0;
    left: 0;
    width: 31vw;
    height: 100vh;
    overflow: hidden;
    display: flex;
    align-items: end;
`

export default {
    Diagonal
}